# Assumptions for now
- [ ] Navbar is manually added into each page as there is currently no logic in place for a dynamic navbar that adapts to who the current user is
    - e.g. `reports.py` could either have a "owner" navbar or an "accounting" navbar but set to "owner" for now
- [ ] List of pages that can't be navigated with buttons as of now (you have to manually navgate to):
    - /inventorydashboard
    - /accountingdashboard
